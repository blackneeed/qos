[package]
name = "qos-rust"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
spin = { version = "0.10.0", default-features = false, features = ["spin_mutex"] }

[build-dependencies]
glob = "0.3"
